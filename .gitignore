.pre-commit-config.yaml
.direnv
tmp
result
latest.iso
.DS_Store

# Editor / tooling backups
*.bak
*.bkp

# Per-machine Claude Code settings
.claude/settings.local.json

# Detailed external topology is local-only; only topology/public.json is safe
# to publish.
topology/private*.json
